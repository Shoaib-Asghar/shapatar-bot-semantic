# Python
__pycache__/
*.py[cod]
*.pyo

# Environment variables
.env

# Mac system files
.DS_Store

# Windows
Thumbs.db

# VS Code
.vscode/

# sentence-transformers downloads models into a local cache.
# These are large binary files (hundreds of MB) that should
# never be committed to Git — they are downloaded automatically
# on first run, not stored in the repo.
.cache/
models/